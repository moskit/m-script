#!/bin/bash
CURL=`which curl 2>/dev/null`
echo "<div id=\"$1\">"
[ -n "$CURL" ] && $CURL -s `head -1 "$PWD/../../standalone/ElasticSearch/${1}.nodes.list"`/_cluster/health | "$PWD/../../../lib/json2html"
echo "</div>"
