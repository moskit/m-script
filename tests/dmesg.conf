# This monitor uses 'dmesg -c' to read the kernel log content, so it deletes
# from the kernel what it reads. But it can send the content to the log file
# assigned here. If this is not defined or empty, the content is sent to 
# /dev/null

DMESGLOG="$M_ROOT/logs/dmesg.log"
