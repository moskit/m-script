# to check/sync certain clusters only (separate by commas)
# by default all clusters from conf/clusters.conf are checked
# CLUSTER=

# Minimal difference in seconds that triggers time adjustment procedure
MAXDELTA=1


